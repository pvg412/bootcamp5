props - попадают в компоненты из вне
    </ Hello
     a='2'>

state - оьъект находится всегда внутри компонента
        изменять state можно только внутри компонента
        служит для работы только текущего компонента или вложенных в него

    state = {

    }

smart - class
        state props
        lifecycle - жизенный цикл

dumb - function
       props

container - cont содержить в себе другие компоненты. обычно хранит глобально. непосредственно отрисовывает.
            app - наглядный пример container

presentation - они должны что-то нарисовать, как-то, что-то отобразить (отрисовать)
               может все менять на основании props

                    разница между компонентом и элементом
                    компонент - как-бы нами написанный кусок кода

state - врнутренние данные компонента
props - из вне компонента
context - специальный вид данных

конструктор компонента






1. выбрать трейлеры трёх любимых фильмов
2. найти их на youtube
3. вывести название этого
4. его авторов
5. сам трейлер вывести



npm i prop-types --save     - команда для установки пакета prop-types

типизация нужня для экономии оперативной памяти
в js сделали утинную типизацию - если оно похоже на строку, значит это строка (тип определяется в момент задачи значения)

в react придумали два вида пакетов для типизации
propTypes       - пакет, заранее прописанных функций на проверку соответсвий
defaultProps    -

    ---task---
написать инпут, у которого будет предустановлен класс со стилями (цвет, шрифт)
и у этого инпута автоматом получается placeholder, value





главное - не путать компоненты с элементами











